{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "dev": "lerna run dev",
    "dev:web": "lerna run dev --scope={@clean-todo-app/web}"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
